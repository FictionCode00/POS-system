import { StubScreen } from "@/components/StubScreen";

export default function InventoryScreen() {
  return (
    <StubScreen
      icon="box"
      title="Inventory"
      blurb="Stock levels, low-stock alerts, and 86'd items sync straight from the POS. Coming in a later phase."
    />
  );
}

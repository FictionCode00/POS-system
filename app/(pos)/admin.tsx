import { StubScreen } from "@/components/StubScreen";

export default function AdminScreen() {
  return (
    <StubScreen
      icon="sliders"
      title="Admin"
      blurb="Menu management, tax settings, staff roles, and reports. This destination lands in a later phase."
    />
  );
}
